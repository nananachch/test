name: Build Crescent Setup Director v0.2.1

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "CSD v0.2.1 verified build archived."
