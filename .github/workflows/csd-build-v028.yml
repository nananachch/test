name: Build Crescent Setup Director v0.2.8

on:
  pull_request:
    branches: [ main ]
    paths:
      - 'CrescentSetupDirectorV028/**'
      - '.github/workflows/csd-build-v028.yml'

permissions:
  contents: read

jobs:
  build:
    runs-on: windows-2022
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Install .NET 10
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x
      - name: Build and verify
        shell: cmd
        run: python CrescentSetupDirectorV028\ci_build_v028.py
      - name: Upload verified artifact
        uses: actions/upload-artifact@v4
        with:
          name: CSD-v0.2.8-verified
          path: CrescentSetupDirectorV028/build-artifact
          if-no-files-found: error
          retention-days: 7
