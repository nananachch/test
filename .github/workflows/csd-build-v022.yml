name: Build Crescent Setup Director v0.2.2 (archived)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "CSD v0.2.2 verified build archived."
